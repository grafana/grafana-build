# Grafana tarball artifact

This is mostly an intermitted artifact that can then be used as source to generate Debian, RPM, or other artifacts.

```
$ dagger run go run ./cmd artifacts -a targz:grafana:linux/amd64
```
