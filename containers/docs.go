// package containers defines individual containers that are used to assemble pipelines.
// running most of these functions that return directories or containers will not actually execute code; container execution is determined by the dagger daemon whenever data is needed from a container.
package containers
